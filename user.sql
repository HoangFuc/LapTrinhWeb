CREATE DATABASE school;
CREATE TABLE inforuser (
	id int PRIMARY KEY,
	mssv varchar(50),
	password varchar(50),
	class varchar(10),
	fullName varchar(10),
	birthday DATE,
	major varchar(10),
	gender varchar(10)
	);
